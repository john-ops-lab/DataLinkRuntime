# Compatibility filename for existing launch commands.
# Resource isolation is now part of docker-compose.yml for every Worker;
# this file does not enable a second execution mode or weaken the base policy.
services: {}
