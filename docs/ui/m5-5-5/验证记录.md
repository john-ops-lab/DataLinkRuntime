# M5.5.5 真实 UI 验证记录（Issue #44）

验证环境：`dlr-ui-verify-28` Compose 栈（web :8890）+ 隔离 fake Provider
（`SMOKE_CHAT_DELAY_SECONDS=6`，仅用于让请求生命周期可观察；compose-smoke 默认关闭）。

驱动方式：headless Chrome（系统 Google Chrome 151）+ CDP，对真实构建产物
（nginx 托管的 vite build）执行真实鼠标/键盘事件；浏览器请求经 Network 域捕获。

## 结论（全部 PASS）

1. Monaco 非空选区（第 4–10 行）后「加入对话上下文」按钮从 disabled 变为可用；
2. 点击后 AI 面板展示标记 `已添加选中文本：第 4–10 行（Python）`；
3. 清除按钮可访问名 `清除已添加的选中文本`，可键盘聚焦（`document.activeElement`），
   真实 Enter 键可激活并移除标记；
4. 「加入对话上下文」按钮以可见文本为可访问名；
5. 请求生命周期进度容器 `role=status`（aria-live），请求挂起阶段展示
   `正在请求 AI 模型…`；
6. 成功收敛到 `已生成修改，等待查看 Diff`，Candidate 卡片生成；
7. 对话文本不含任何 reasoning 内容；
8. 浏览器实际发出的 assist 请求携带结构化选区快照：

```json
{
  "text": "ext.logger.info(\"开始处理\")\n    items = input.get(\"items\", []) ...\n",
  "start_line": 4,
  "end_line": 10
}
```

## 截图

| 文件 | 内容 |
|---|---|
| `01-Monaco选中代码.png` | Monaco 中第 4–10 行处于选中高亮状态 |
| `02-加入对话上下文标记.png` | AI 面板显示「已添加选中文本：第 4–10 行（Python）」+ 清除按钮 |
| `03-请求阶段进度.png` | 请求挂起期间的平台阶段进度「正在请求 AI 模型…」 |
| `04-Candidate生成完成.png` | Candidate 生成完成 + 「已生成修改，等待查看 Diff」收敛态 |

（1440×813，经像素抽样确认非空白。）
