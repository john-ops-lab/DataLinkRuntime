# M5.11 Wave C 真实浏览器验收记录（Issue #107）

基线：`origin/main@683de0168b060fa1af4e4bf635a1be843018c41c`。本记录只覆盖
Wave C：Adapter 设置、永久删除/停止删除安全门禁，以及 Webhook Bearer Token
语义；不包含 Wave D。

## 环境与数据边界

- 使用真实 Chromium + Playwright，访问 fresh disposable Compose project
  `dlr-m511-wave-c-72`（Control `:8892`、Account `:8893`）。
- 所有 Adapter、用户、Credential 和执行均为本地 disposable fixture；示例字段只使用
  `EXAMPLE_*` 占位值。报告和截图不包含 Token、密码、密钥、Credential ciphertext 或
  Worker secret。
- 截图来自真实渲染页面，不是 jsdom 或单元测试截图。

## 结果

| 范围 | 覆盖 | 结果 |
|---|---|---|
| 设置抽屉 | `zh-CN`/`en`；1280/1440/1680/1920；窄屏 520；摘要、只读语言、3–4 行描述、固定底栏、未保存关闭确认、同抽屉权限往返、复制历史说明、永久删除说明 | PASS |
| 权限差异 | Account `read` / `edit`，`zh-CN`/`en`，1280；编辑能力、只读提示、复制/删除可见性 | PASS |
| 删除生命周期 | 普通永久删除；pending 停止删除；running 经 Worker `cancelled` 后停止删除；Worker 离线安全阻断；Credential 本体保留 | PASS |
| Webhook | Bearer Token 提示；无入口 Token/Worker 仍可保存 Revision/配置；仅 Start receiving 检查 Token、Worker、已保存 Revision；Starter Code 不读取 `context.secrets` 中的入口 Token | PASS |

## 诊断与证据

- 主报告：`browser-report.json`（12 个设置抽屉记录 + 2 个 Webhook 运行设置记录）。
- 权限报告：`permission-browser-report.json`（4 个 Account 权限记录）。
- 截图目录：`browser/`，包含全部语言/宽度/权限状态截图。
- 所有记录的 `console_errors`、`page_errors`、`unknown_requests` 和 HTTP 5xx 为空；
  `documentScrollWidth === innerWidth` 且 `bodyScrollWidth === innerWidth`。
- 停止删除收尾中的 `aborted_requests` 仅是浏览器在删除/页面切换时主动取消的
  Execution watcher 请求；真实 HTTP `failed_requests` 与 HTTP 5xx 均为空。
- 停止删除记录确认 Worker 先取消再删除；浏览器中未执行 Worker 离线强制清理。
