# M5.7 Wave D 最终 i18n / 可访问性 / 安全回归与真实浏览器验收记录（Issue #80）

验证基准：`main@aae1f5da1f6a25e2fc9694986d88fadafdeed21c`（#82～#87 全部合并后的
exact main）。本分支 `ao/datalinkruntime-54/wave-d-final` 从该 SHA 独立创建，无共享
本地 main 依赖。

## 一、前置核验（实时）

| Issue | 标题 | 状态 | 对应 PR | PR 状态 |
|---|---|---|---|---|
| #80 | M5.7 AI Assistant UI 组件化与附件 / Tool Call / MCP 扩展 | OPEN | #82~#87（Wave A~C2） | MERGED |
| #20 | M5（父 Issue） | OPEN | — | — |

`git log origin/main` 顶部恰为 aae1f5d（#87 的 merge commit）。本 Wave 不关闭 #80/#20，
不进入 M6。

## 二、环境边界（真实浏览器）

- **AO desktop 桥实测**（本会话）：`open` / `snapshot` / `fill` 可用；`click` 无法把
  事件送达页面（登录点击后仍停留登录页）。与 M5.5.14 / M5.5.6 / M5.5.13 记录的已知
  限制一致，未无限重试。
- **验收驱动**：按项目已验证回退方案，使用 **Playwright 1.62.1 + 系统 Google Chrome
  （真实 Chromium 内核，headless，真实鼠标/键盘/选区事件）**，驱动**同一套真实
  compose 栈**（`dlr-ui-verify-54`，web :8894，nginx 托管真实 `vite build` 产物，
  postgres 16 + Alembic fresh 迁移链 0001→0019 + worker 真实执行）。
- **AI Provider**：隔离 fake Provider（容器内 `scripts/ai-fake-provider.py`，
  `dlr-smoke-model`，工具场景按浏览器可见消息标记触发，不访问任何公网模型）。
- **ima**：隔离 fake 官方 ima 兼容服务（`scripts/ima-fake-service.py`，官方
  OpenAPI 契约 /openapi/wiki/v1 + `ima-openapi-clientid` / `ima-openapi-apikey`
  请求头），凭据经 DLR access_key Credential（Secret Store）解析。
- 全部截图来自该真实浏览器；截图经像素抽样确认非空白；页面正文 Secret 扫描通过
  （管理员 Token / ima Client ID / ima API Key / 附件正文哨兵均未出现在页面文本）。
- 真实 ima POC：用户已手工确认真实 ima 端到端验证通过；本记录仅登记为人工验收
  证据，不索取、不读取、不写入、不回显任何真实 API Key / Client ID。

## 三、Wave D 代码改动（最小必要，无架构扩张）

| 文件 | 改动 |
|---|---|
| `web/src/i18n/locales/zh-CN/common.json` | 补 `ai_tool_unsupported` / `ai_tool_limit_exceeded` / `ai_tool_result_too_large` 三个稳定错误码中文文案（与后端 `_TOOL_ERRORS` 兼容消息一致） |
| `web/src/i18n/locales/en/common.json` | 同上英文文案（key 与占位符双语一致） |
| `web/src/components/AiAssistantPanel.tsx` | 面板错误路径把 `ai_tool_*` 稳定码纳入 common.errors 双语本地化（与 `ai_attachment_*` 同一合同；错误详情仍只带稳定码，不回显工具参数/结果/Secret） |
| `web/src/index.css` | 删除从未被引用的死代码 `.ai-message-content`（Wave A 引入但从未接线到任何 TSX） |
| `web/src/assistant-wave-d.test.tsx`（新增） | Wave D 回归测试：M5.7 稳定错误码 common.errors 双语 key 一致性；`ai_tool_*` 面板错误 zh/en 渲染；整个 assistant 链 ai namespace 双语 key 一致性；a11y 可访问名称全集（打开/关闭/发送/附件添加/上下文片段）；键盘 Enter/Shift+Enter/Ctrl+Meta+Enter；候选 Apply 禁用 reason 机器可读；附件 base64 正文与哨兵文本永不进会话 DOM；工具摘要仅渲染服务端脱敏摘要；超大摘要客户端二次截断 |

## 四、自动验证结果

| 门禁 | 结果 |
|---|---|
| Backend ruff check / format check | PASS（`All checks passed!` / `105 files already formatted`） |
| Backend mypy | PASS（`Success: no issues found in 72 source files`） |
| Backend full pytest | PASS（592 passed） |
| Web lint / typecheck | PASS |
| Web full vitest | PASS（21 files / 284 tests，含 Wave D 新增 10 tests） |
| Web build | PASS（`✓ built`） |
| compose-smoke（含 M5.7 附件/原生图片/拒绝、Tool Call 单/多/未知/写拒绝、ima list→search→read、Secret 日志脱敏断言） | PASS（`compose smoke test passed`） |

## 五、真实浏览器验收（Issue #80 第 29/28 项 + 主要交互）

| # | 验收项 | 结果 | 截图（zh）/（en） | 关键断言 |
|---|---|---|---|---|
| 1 | 中文长文案 1280/1440/1680/1920 无横向破版 | PASS | 01-zh-long-copy-{width} | `scrollWidth <= clientWidth`，innerWidth 与视口一致 |
| 2 | 普通问答 + Candidate 渲染 | PASS | 02-zh-candidate | fake Provider 确定性候选摘要可见 |
| 3 | Diff 弹窗 | PASS | 03-zh-diff | 三栏 diff 打开（代码/依赖/运行参数） |
| 4 | Apply 只改浏览器 Working Copy | PASS | 04-zh-applied | `ai-candidate-applied` 状态出现 |
| 5 | Regenerate（冻结轮次快照） | PASS | 05-zh-regenerate | 重新生成后新回复替换旧回复 |
| 6 | 附件（文本/markdown）选择与发送 | PASS | 06/07-zh-attachment* | 附件行出现；`with attachment` 证明解析文本到达 Provider；附件正文哨兵不出现在页面文本 |
| 7 | 受控只读 Tool Call UI | PASS | 08-zh-tool-call | `ai-tool-call` 卡片出现（成功状态、脱敏参数/结果摘要） |
| 8 | ima 知识摘要（list→search→read） | PASS | 09-zh-ima-summary | 知识工具卡片渲染；`with knowledge result` 证明三连读链完成 |
| 9 | 英文长文案 1280/1440/1680/1920 无横向破版 | PASS | 10-en-long-copy-{width} | 同上 |
| 10 | 英文 Q&A + Candidate | PASS | 11-en-candidate | 英文界面确定性候选摘要 |
| 11 | 英文 Diff | PASS | 12-en-diff | 英文标签 diff |
| 12 | 英文 Regenerate | PASS | 13-en-regenerate | 英文界面重新生成 |
| 13 | 英文 Tool Call | PASS | 14-en-tool-call | 英文工具卡片 |
| 14 | 英文 ima 摘要 | PASS | 15-en-ima-summary | 英文知识工具卡片 |
| 15 | Secret 不进页面正文 | PASS | — | 管理员 Token / ima Client ID / ima API Key / 附件哨兵均未出现在 `body.innerText` |
| 16 | 无 console / page error | PASS | — | 全程 0 错误 |

注：本批截图使用隔离 fake 服务（无真实 Key、无私有知识内容），符合 Issue #80
「真实公网 AI / ima 验证不得把真实 Key、附件正文或知识库私有内容写入仓库、测试
fixture、PR 日志或截图」的约束；真实 ima POC 的人工验收证据见第二节。

## 六、回归合同核对（零回退）

- 既有 Task / Webhook / Revision / Runtime Lock / Secret 合同：backend full pytest
  （592）+ compose-smoke 的 M5.4.4 链 + web 全量 vitest 全部通过；
- Secret 真值不进 Prompt / UI / Tool 日志：compose-smoke 日志断言 + 浏览器页面正文
  扫描 + wave-c2 后端测试保持通过；
- SSRF / 只读边界：`test_ai_knowledge.py`（主机白名单 / IP 字面量 / 明文 http 逃逸
  闩 / userinfo / query / 重定向拒绝 / media URL 守卫 / 写风格鸭子类型拒绝）全部通过；
- recent_messages 仍最多 8 条、Regenerate 冻结轮次、附件不跨 Adapter、late response
  不跨 Adapter：wave-b1/b3/c1/c2 测试全部通过。

## 七、STOP 合同

本 Wave 不关闭 #80 / #20，不进入 M6；等待用户 M5.7 最终人工验收。
