# Issue #127 E1 retained app handoff

Status: `HISTORICAL / SUPERSEDED`.

`app-ready.json` points to the old `dlr-i127-e0-141` stack and does not represent
the current repaired tree. It is retained only as a historical machine-health
record. Task 20.1 is reopened; a new exact-SHA app must be built and checked
before any current `APP_READY` handoff.

The old receipt stores no admin token or account password. It provides no
current handoff values. Tasks 20.1–20.3 remain incomplete and no user acceptance
is claimed. Human state for the current Candidate: `NOT_STARTED`.
